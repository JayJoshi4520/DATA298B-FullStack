# ======================
# CORE CONFIGURATION
# ======================
NODE_ENV=development
PORT=3030
PROJECT_ROOT=/home/coder/project
COMMAND_TIMEOUT=30000
MAX_FILE_SIZE=1048576

# Development options
DEV_MODE=true
CONTENT_DEV_MODE=true
ENABLE_MOCK_AI=false
DEBUG=true

# ======================
# GOOGLE CLOUD RESOURCES (Firestore / GCS)
# ======================
# Required for Firestore and Cloud Storage even if using API Key for LLM
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GCS_BUCKET_NAME=your-bucket-name
# Credential file path inside container (mounted via docker-compose)
GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/sa.json

# ======================
# LLM PROVIDER CONFIGURATION
# ======================
# Options: vertexai, google (for AI Studio), openai, etc.
LLM_PRIMARY_PROVIDER=vertexai
LLM_MODEL=YOUR_MODEL_NAME

# --- Option 1: Google AI Studio (API Key) ---
# Set to false to use API Key
GOOGLE_GENAI_USE_VERTEXAI=false
GOOGLE_API_KEY=your-api-key
LLM_BASE_URL=https://generativelanguage.googleapis.com

# --- Option 2: Vertex AI (ADC / Service Account) ---
# Set to true to use Vertex AI
# GOOGLE_GENAI_USE_VERTEXAI=true
# (Uses GOOGLE_APPLICATION_CREDENTIALS from above)
